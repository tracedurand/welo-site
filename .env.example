DATABASE_URL=postgresql://postgres:postgres@localhost:5432/welo_site
# Local: disable. Hosted (Heroku Postgres, etc.): require — or rely on NODE_ENV=production.
PGSSLMODE=disable
# Heroku sets NODE_ENV=production automatically; that enables SSL for pg without PGSSLMODE.
